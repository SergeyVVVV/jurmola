# 🤖 Автоматическая генерация новостей

## Как это работает

Каждый день в 9:00 UTC (12:00 по времени Риги) GitHub Action автоматически:
1. Генерирует новую сатирическую новость через OpenAI API
2. Переводит её на латышский и русский языки
3. Добавляет в код сайта
4. Коммитит изменения
5. Vercel автоматически деплоит обновление

## 📋 Настройка (один раз)

### 1. Добавьте OpenAI API ключ в GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Введите:
   - **Name:** `OPENAI_API_KEY`
   - **Secret:** ваш ключ (начинается с `sk-proj-...`)
5. Нажмите **Add secret**

### 2. Установите зависимости локально

```bash
npm install
```

### 3. Готово! 🎉

GitHub Action будет автоматически генерировать новость каждый день.

## 🧪 Тестирование

### Локальный запуск (для тестирования)

Убедитесь, что файл `.env.local` содержит ваш API ключ:

```bash
# В корне проекта создайте .env.local
OPENAI_API_KEY=sk-proj-xxxxxx
```

Затем запустите генерацию:

```bash
npm run generate-article
```

Скрипт:
1. ✅ Сгенерирует новую статью на английском
2. ✅ Переведёт на латышский
3. ✅ Переведёт на русский  
4. ✅ Добавит в код (в начало списка статей)
5. ✅ Установит статью как featured на главной странице

### Ручной запуск через GitHub

1. Откройте **Actions** в вашем репозитории
2. Выберите **Generate Daily Article**
3. Нажмите **Run workflow** → **Run workflow**

## 📅 Изменить расписание

Отредактируйте `.github/workflows/generate-daily-article.yml`:

```yaml
on:
  schedule:
    # Формат: минута час день месяц день_недели
    - cron: '0 9 * * *'  # 9:00 UTC = 12:00 Riga (летнее время)
```

Примеры:
- `'0 6 * * *'` - 6:00 UTC (9:00 Riga)
- `'0 12 * * *'` - 12:00 UTC (15:00 Riga)
- `'0 18 * * *'` - 18:00 UTC (21:00 Riga)

## 💰 Стоимость

Одна статья (генерация + 2 перевода) стоит ~$0.05-0.15 в зависимости от длины.

Примерно **$1.50-4.50 в месяц** за ежедневные новости.

## 📰 SEO-оптимизация: Статьи на основе актуальных новостей

Для лучшего SEO и трафика, вы можете создавать сатирические статьи на основе актуальных новостей из:
- **https://balticnews.com/** 
- **https://www.baltictimes.com/**

### Как использовать:

1. **Перед генерацией** - зайдите на сайты выше и выберите самую горячую новость дня
2. **Запустите генерацию с темой:**

```bash
REAL_NEWS_TOPIC="City of Tartu considering subsidizing morning bus connections to Riga" npm run generate-article
```

Или добавьте в `.env.local`:
```bash
REAL_NEWS_TOPIC="Your trending news topic here"
```

**Преимущества:**
- ✅ Статьи релевантны актуальным поисковым запросам
- ✅ Больше трафика через SEO
- ✅ Сатира на основе реальных событий более интересна
- ✅ Статьи попадают в поиск по актуальным темам

**Примеры тем:**
- "Latvia supports Paris Declaration on Ukraine"
- "Fuel prices decline in Riga, up in Tallinn and Vilnius"
- "Estonia mulling social tax change, implications for couriers"

## 🎨 Кастомизация

### Изменить стиль статей

Отредактируйте промпт в `scripts/generate-article.ts`:

```typescript
const prompt = `You are a writer for "Jurmola Telegraphs"...`;
```

### Добавить больше категорий

В `scripts/generate-article.ts`:

```typescript
const categories = [
  { en: "Sports", lv: "Sports", ru: "Спорт" },
  // ...
];
```

### Изменить авторов

```typescript
const authors = [
  { en: "By Your Name", lv: "Rakstījis Your Name", ru: "Автор: Your Name" },
  // ...
];
```

## 🖼️ Каверы статей

**Источники картинок:**
- **Новые статьи** (через `generate-article`): [Picsum Photos](https://picsum.photos/) — `https://picsum.photos/seed/{id}/800/600` (уникальный seed = id статьи).
- **Существующие статьи** в `app/data/articles.ts`: те же Picsum (seed = id) для внешних URL; локальные `/images/...` не трогаются.

**Исправить дубликаты / битые каверы:** заменить все внешние `imageUrl` на уникальные Picsum:

```bash
npm run assign-covers
```

Скрипт оставляет без изменений только локальные пути вида `/images/...`.

## 🐛 Troubleshooting

### "OPENAI_API_KEY is not set"

Убедитесь, что:
- Локально: файл `.env.local` существует и содержит ключ
- GitHub: Secret `OPENAI_API_KEY` добавлен в настройках репозитория

### GitHub Action не запускается

1. Проверьте вкладку **Actions** - включены ли workflows
2. Проверьте, что репозиторий не private (или есть Actions минуты)
3. Попробуйте запустить вручную через **Run workflow**

### Статья не появляется на сайте

1. Проверьте, что коммит был сделан (вкладка Commits)
2. Проверьте, что Vercel успешно задеплоил изменения
3. Очистите кеш браузера (Cmd+Shift+R / Ctrl+Shift+R)

## 📧 Поддержка

Если что-то не работает - проверьте вкладку **Actions** на GitHub для логов.

